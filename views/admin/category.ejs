<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>category</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400&display=swap" rel="stylesheet">

    <!-- Favicon -->
    <link rel="shortcut icon" type="image/x-icon" href="/assets/imgs/theme/favicon.svg" />
    <!-- Template CSS -->
    <!-- <link href="/assets/css/main.css" rel="stylesheet" type="text/css" /> -->
    <link rel="stylesheet" href="/assets/css/main.css">
    <!-- jQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <!-- //pagination style -->
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.2/css/jquery.dataTables.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>

<body>
    <div class="screen-overlay"></div>
    <aside class="navbar-aside" id="offcanvas_aside">
        <div class="aside-top">
            <a href="index.html" class="brand-wrap">
                <img src="/images/shous-logo.png" class="logo" alt="Nest Dashboard" />
            </a>
            <div>
                <button class="btn btn-icon btn-aside-minimize"><i
                        class="text-muted material-icons md-menu_open"></i></button>
            </div>
        </div>
        <nav>
            <ul class="menu-aside">
                <li class="menu-item">
                    <a class="menu-link" href="/admin/index">
                        <i class="icon material-icons md-home"></i>
                        <span class="text">Dashboard</span>
                    </a>
                </li>
                <li class="menu-item has-submenu">
                    <a class="menu-link" href="/admin/users">
                        <i class="icon material-icons md-shopping_bag"></i>
                        <span class="text">users</span>
                    </a>

                </li>
                <li class="menu-item has-submenu active">
                    <a class="menu-link" href="/admin/category">
                        <i class="icon material-icons md-shopping_cart"></i>
                        <span class="text">Category</span>
                    </a>
                </li>

                <li class="menu-item has-submenu">
                    <a class="menu-link" href="/admin/product">
                        <i class="icon material-icons md-add_box"></i>
                        <span class="text">products</span>
                    </a>
                </li>
                <li class="menu-item ">
                    <a class="menu-link" href="/admin/orders">
                        <i class=" icon  fa-solid fa-boxes-stacked"></i>
                        <span class="text">Orders</span>
                    </a>
                </li>
                <li class="menu-item ">
                    <a class="menu-link" href="/admin/coupon">
                        <!-- <i class="icon material-icons md-shopping_cart"></i> -->
                        <!-- <i class=" fa-solid fa-ticket"></i> -->

                        <i class="icon fa-solid fa-ticket"></i>
                        <span class="text">Coupon</span>
                    </a>
                </li>
                <li class="menu-item ">
                    <a class="menu-link" href="/admin/banner">

                        <i class="icon fa-regular fa-object-group"></i>
                        <span class="text">Banner</span>
                    </a>
                </li>
                <li class="menu-item ">
                    <a class="menu-link" href="/admin/sales">

                        <i class="icon fa-solid fa-border-all"></i>
                        <span class="text">Sales</span>
                    </a>
                </li>
                <li class="menu-item ">
                    <a class="menu-link" href="/admin/offer">

                        <i class="icon fa-solid fa-tags"></i>
                        <span class="text">Offer</span>
                    </a>
                </li>

               

            </ul>
            <hr />
            <ul class="menu-aside">
                <li class="menu-item">
                    <a class="menu-link" href="page-blank.html">
                        <i class="icon material-icons md-local_offer"></i>
                        <span class="text"> Starter page </span>
                    </a>
                </li>
            </ul>
            <br />
            <br />
        </nav>
    </aside>
    <main class="main-wrap">
        <header class="main-header navbar">
            <div class="col-search">
                <div class="content-header mt-2 mb-0">
                    <h2 class="content-title">Category</h2>
                </div>

            </div>
            <div class="col-nav">
                <button class="btn btn-icon btn-mobile me-auto" data-trigger="#offcanvas_aside"><i
                        class="material-icons md-apps"></i></button>
                <ul class="nav">



                    <li class="dropdown nav-item">
                        <a class="dropdown-toggle" data-bs-toggle="dropdown" href="#" id="dropdownAccount"
                            aria-expanded="false"> <img class="img-xs rounded-circle"
                                src="/assets/imgs/people/avatar-2.png" alt="User" /></a>
                        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownAccount">

                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item text-danger" href="/admin/logout"><i
                                    class="material-icons md-exit_to_app"></i>Logout</a>
                        </div>
                    </li>

                </ul>
            </div>
        </header>
        <section class="content-main">

            <div class="content-header" style="justify-content: end">

                <div class="d-flex justify-content-center">
                    <a href="/admin/addcategory" class="btn btn-primary"><i
                            class="text-muted material-icons md-post_add"></i>Add
                        Category</a>
                </div>
            </div>

            <div class="pagination-area mt-30 mb-50 usertable">
                <table id="tablePagination" class="table table-hover">
                    <thead style="background-color: #3BB77E;">
                        <tr>
                            <th scope="col" class="th-header">ID</th>
                            <th scope="col" class="th-header">Name</th>
                            <th scope="col" class="th-header">Description</th>
                            <th scope="col" class="th-header">Status</th>
                            <th scope="col" class="th-header">Offer</th>
                            <th scope="col" class="th-header">Edit</th>
                            <th scope="col" class="th-header">Action</th>
                        </tr>
                    </thead>
                    <tbody>

                        <% if (NewCategory.length> 0) { %>
                            <% for (let i=0; i < NewCategory.length; i++) { %>
                                <tr>
                                    <td>
                                        <%= i + 1 %>
                                    </td>

                                    <td>
                                        <b>
                                            <%= NewCategory[i].name %>
                                        </b>
                                    </td>

                                    <td>
                                        <b>
                                            <%= NewCategory[i].discription %>
                                        </b>
                                    </td>


                                    <td>
                                        <b>
                                            <%= NewCategory[i].is_list%>
                                        </b> 
                                    </td>
                                    <td>
                                        <b>
                                            <% if(NewCategory[i].offer){ %>
                                                <a>
                                                    <button type="button" class="removeoffer"
                                                        onclick="removecategoryoffer('<%=i%>')">Remove</button></a>
                                                <input type="hidden" value='<%= NewCategory[i]._id %>'
                                                    id="hiddencategoryid<%=i%>">
                                                <%}else{%>
                                                    <a>
                                                        <button type="button" class="applyoffer"
                                                            onclick="showofferModal('<%=i%>')"> APPLY</button>
                                                    </a>
                                                    <input type="hidden" value='<%= NewCategory[i]._id %>'
                                                        id="hiddencategoryid<%=i%>">

 
                                                    <%}%>
                                        </b>
                                    </td>
                                    <td>
                                        <b>
                                            <input type="hidden" value="<%= NewCategory[i]._id %>" id="hiddenid">
                                            <button class="Edit-category"
                                                onclick="confirmationeditModal('<%= NewCategory[i].name %>',' <%= NewCategory[i].discription %>')">EDIT
                                            </button>
                                            
                                        </b>
                                    </td>

                                    <td>
                                        <% if (NewCategory[i].is_list) { %>
                                            <button
                                                onclick="showConfirmationModal('<%= NewCategory[i]._id %>','<%= NewCategory[i].is_list %>')"
                                                class="unblock-category"
                                                id="blockButton<%=NewCategory[i]._id %>">unblock</button>
                                            <% } else { %>
                                                <button onclick="showConfirmationModal('<%= NewCategory[i]._id %>')"
                                                    class="block-category"
                                                    id="blockButton<%= NewCategory[i]._id %>">block</button>
                                                <% } %>
                                    </td>

                                </tr>
                                <% } %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="4">
                                                <p>No Categories found in the database</p>
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>

            <section>
                <!-- Modal -->
                <div class="modal fade" id="confirmationModal" tabindex="-1" aria-labelledby="confirmationModalLabel"
                    aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="confirmationModalLabel">Confirmation</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                    aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                Are you sure ?
                            </div>
                            <input type="hidden" name="" id="modal-prodId">
                            <input type="hidden" name="" id="modal-prodList">
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="button" class="btn btn-danger" id="mbid"
                                    onclick="confirmBlockCategory()"></button>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <section>

                <!-- edit category Modal -->

                <div class="modal fade" id="confirmationeditModal" tabindex="-1"
                    aria-labelledby="confirmationeditModalLabel" aria-hidden="true">

                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">

                                <h5 class="modal-title" id="confirmationeditModalLabel">Confirmation</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                    aria-label="Close"></button>


                            </div>
                            <p id="added" style="text-align: center; color: red; display: none;">this category is
                                already added</p>
                            <div class="modal-body">
                                <% if(typeof message !=='undefined' ) {%>
                                    <p class="">
                                        <%=message%>
                                    </p>
                                    <%}%>

                                        <div class="card-body">
                                            <form method="post" id="editCategoryForm">
                                                <div class="mb-4">
                                                    <label for="product_name" class="form-label">Category name</label>
                                                    <input type="text" placeholder="Type here" name="editname"
                                                        class="form-control " id="editcategoryname" />
                                                    <span id="edit-category-name" style="color: red;"></span>

                                                </div>
                                                <div class="mb-4">
                                                    <label class="form-label">Full description</label>
                                                    <input type="text" name="editdiscription" placeholder="Type here"
                                                        class="form-control " id="editcategorydescription" />
                                                    <span id="edit-category-desc" style="color: red;"></span>

                                                </div>

                                            </form>
                                        </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-primary" onclick="editcategoryvalidation()"><i
                                        class="text-muted material-icons md-post_add"></i>submit</button>
                            </div>
                        </div>
                    </div>
                </div>

            </section>

            <!--  -->

            <!--offer Modal -->

            <section>
                <div class="modal fade" id="offerconfirmationModal" tabindex="-1"
                    aria-labelledby="confirmationModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="confirmationModalLabel">Confirmation</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                    aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                <% if (offer.length> 0) { %>
                                    <div class="row " style="justify-content: space-between;">
                                        <% for (let i=0; i < offer.length; i++) { %>
                                            <div class="col-md-6 mb-4 p-0 ">
                                                <div class="card m-3">
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <%= offer[i].name %>
                                                        </h5>
                                                        <p class="card-text">Discount: <%= offer[i].discountAmount %>
                                                        </p>
                                                        <p class="card-text">Activation Date: <%=
                                                                offer[i].activationDate.toLocaleDateString('en-US', {
                                                                year:'numeric', month: 'short' , day: '2-digit'
                                                                }).replace(/\//g, '-' ) %>
                                                        </p>
                                                        <p class="card-text">Expiry Date: <%=
                                                                offer[i].expiryDate.toLocaleDateString('en-US', {
                                                                year:'numeric', month: 'short' , day: '2-digit'
                                                                }).replace(/\//g, '-' ) %>
                                                        </p>

                                                        <button class="btn Edit-category"
                                                            onclick="categoryofferapply('<%= offer[i]._id %>')">Apply</button>
                                                    </div>
                                                </div>
                                            </div>
                                            <% } %>
                                    </div>
                                    <% } else { %>
                                        <p class="text-center">No offers available</p>
                                        <% } %>
                            </div>
            </section>


            <script>

                // offer js

                let index;
                function showofferModal(i) {
                    index = i;
                   
                    if (status == "true") {

                        document.getElementById('mbid').innerText = 'unblock'
                        document.getElementById('mbid').style.backgroundColor = '#ed6f56';

                    } else {
                        document.getElementById('mbid').innerText = 'block'
                        document.getElementById('mbid').style.backgroundColor = '#29A56C';
                    }
                    $('#offerconfirmationModal').modal('show');
                }

                function confirmBlockUser() {

                    const userId = $('#offerconfirmationModal').data('userId');
                    console.log(userId);
                    blockUser(userId)

                    $('#offerconfirmationModal').modal('hide');
                }

                // edit category validation 

                function editcategoryvalidation() {
                    event.preventDefault();


                    const categoryname = document.getElementById('editcategoryname').value;
                 
                    const editdiscription = document.getElementById('editcategorydescription').value;



                    const validateCategoryName = (categoryname, editdiscription) => {


                        document.getElementById('edit-category-name').textContent = '';
                        document.getElementById('edit-category-desc').textContent = '';
                        if (categoryname && editdiscription) {

                        

                            const validateCategoryName = (categoryname) => {
                                const categoryRegex = /^[a-zA-Z0-9]+\s*[a-zA-Z0-9]*$/;
                                return categoryRegex.test(categoryname);
                            };

                            const isValid = validateCategoryName(categoryname);



                            if (isValid) {
                                confirmEditCategory(categoryname);
                            }
                        } else {
                            if (categoryname.trim() === "") {
                                document.getElementById('edit-category-name').textContent = "Please enter a name";

                            }

                            if (editdiscription.trim() === "") {
                                document.getElementById('edit-category-desc').textContent = "Please enter a description";

                            }
                        }
                    };
                    const isvalidatae = validateCategoryName(categoryname, editdiscription);
                    if (isvalidatae) {
                        console.log("category name is valid");
                    } else {
                        console.log("invalid category name");
                    }
                }



                function blockCategory(categoryId) {
              
                    const blockButton = document.getElementById(`blockButton${categoryId}`);
                    let text = blockButton.innerHTML;
                    if (text === 'block') {
                      
                        fetch(`/admin/blockCategory`, {
                            method: 'PUT',
                            headers: {
                                "Content-Type": "application/json"
                            },

                            body: JSON.stringify({ categoryId })
                        })
                            .then(response => response.json())
                            .then(data => {
                                window.location.reload();

                            })
                            .catch(error => {
                                console.log("error", error);
                            });
                    }

                    else if (text === "unblock") {
                        fetch(`/admin/unblockCategory`, {
                            method: 'PUT',
                            headers: {
                                'Content-Type': "application/json"
                            },
                            body: JSON.stringify({ categoryId })

                        }).then(response => response.json())
                            .then(data => {
                                window.location.reload();
                            })
                            .catch(error => {
                                console.error("error", error);
                            })
                    }
                }

            </script>

            <script>
                function showConfirmationModal(categoryId, status) {
                    //button text changing
                   
                    if (status == "true") {
                        document.getElementById('mbid').innerText = 'unblock'
                    } else {
                        document.getElementById('mbid').innerText = 'block'
                    }
                    document.querySelector('#modal-prodId').value = categoryId
                    $('#confirmationModal').data('categoryId', categoryId);

                    $('#confirmationModal').modal('show');
                }
                function confirmBlockCategory() {
                    const categoryId = document.querySelector('#modal-prodId').value;
                    blockCategory(categoryId)
                    $('#confirmationModal').modal('hide');
                }




                function confirmationeditModal(catname, catdisc) {

               
                    $('#confirmationeditModal').modal('show');



                    document.querySelector('[name="editname"]').value = catname;
                    document.querySelector('[name="editdiscription"]').value = catdisc;

                }

                function confirmEditCategory(categoryname) {
              
                    editcategory(categoryname);
                }

                function editcategory(categoryname) {
                    const id = document.getElementById('hiddenid').value;
                 

                    

                    const editcategoryname = document.getElementById('editcategoryname').value;
                    const editcategorydesc = document.getElementById('editcategorydescription').value;



                    fetch(`/admin/editcategory/${id}`, {
                        method: 'PUT',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({
                            editcategoryname,


                        })
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.status === "failed") {

                                document.getElementById("added").style.display = "block";

                            } else {
                                window.location.href = "/admin/category";

                            }


                        }).catch(error => {
                            console.log(error);

                        })





                }

            </script>



        </section>


        </section>
        <!-- content-main end// -->
        <footer class="main-footer font-xs">
            <div class="row pb-30 pt-15">
                <div class="col-sm-6">
                    <div class="text-sm-end">All rights reserved</div>
                </div>
            </div>
        </footer>
    </main>





    <!-- pagination jQuery links and js -->
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://cdn.datatables.net/1.13.2/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" charset="utf8"
        src="https://cdn.datatables.net/1.13.2/js/jquery.dataTables.js"></script>
    <script>
        $(document).ready(function () {
            $('#tablePagination').DataTable();
        });
    </script>

    <!-- validation form -->
    <script src="/validation/validation.js"></script>
    <script src="/js/fetch.js"></script>



</body>

</html>